# 三方社区开发规范（DEV_GUIDELINES）

本仓库（SCU URP++ 主题/插件商店）的第三方内容开发规范。投稿主题/插件、修 bug、提交更新前，请先读一遍。

## 一、主题投稿（theme）

1. 主题只注入 CSS，必须以 `html[data-urppp-skin="<id>"]` 为前缀，不执行任意 JS。
2. 需要脚本能力走 plugin 类型，并声明 `allowJS`。
3. 版本号用语义化版本（`x.y.z`），用于「检查更新」对比；**每次改动必须 bump 版本**，否则用户检查更新不生效。
4. **卡片样式（cardCss）必填**：catalog 项里带 `cardCss`，作用于 `.urppp-skin-card[data-skin="<id>"]`，用于商店展示。
5. **暗色适配必做**：若主题支持暗色（有 `dark: true`），`cardCss` 必须提供 `html.urppp-theme-dark` 暗色变体，且**同时覆盖卡片底色、文字色、主按钮（`.urppp-skin-apply`）、次要按钮（`.urppp-set-btn.ghost`）**。只适配亮色会在暗色下残留白底/亮色残留。
6. **按钮点缀克制**：卡片主按钮（使用/下载）可随主题特色，但**次要按钮（仓库/删除）要克制**——细边框 + 透明底 + 与卡片文字同色即可，不要大面积色块/硬阴影，否则与整体设置面板不协调。
7. 悬停态：背景变化要**明显**且**文字同步换色**（卡片、名称、描述一起），避免「背景变了文字却没跟」或「只改文字不改背景」导致看不清。

## 二、CSS 常见坑（修 bug 必看）

- **`!important` 覆盖关系**：全局 `.urppp-set-btn.ghost` 带了 `background/color/border` 的 `!important`（用当前主题变量）。想让某个卡片的按钮随卡片主题，**必须用更高 specificity + 同样的 `!important`**，否则会被全局压掉，显示成「当前应用主题」的颜色。
- **同一份 `!important` 按 specificity 再按声明顺序**：相同 specificity 时，后声明的胜。**新规则必须放在旧规则后面**，否则不生效（之前 flat 卡暗色被旧规则盖住就是这原因）。
- **不要重复定义**：同一选择器（如某主题的暗色卡）只写一份。重复的、以及被后面规则覆盖的（死代码）要删掉，否则体积只增不减。

## 三、主题状态与 UI

- **应用/删除主题后，立即刷新对应列表**（重渲染），否则「使用中」「已下载」等状态徽标不会即时更新。
- 删除正在应用的主题时，**要回落到默认主题**（apple），不能靠残留缓存继续加载。

## 四、修 bug 纪律

1. **修一次，回望一次**：修完后回头扫一遍改动，删掉冗余/重复/死代码，让体积只减不增。
2. 改动前先确认根因（看是不是历史遗留的重复规则覆盖），**不要靠反复打补丁**，容易越修越大。
3. 每次改动记得在 `CHANGELOG.md` 记录，主插件版本与代码同步 bump。

## 五、插件投稿补充

- 插件需声明 `allowJS`，catalog 里给出 `entry`（多源、主插件按序降级）与 `repo`。
- 插件名、版本、描述、作者需与注册时一致，便于商店管理页正确显示。
